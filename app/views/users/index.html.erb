<h1>User info</h1>
    <p>
        name
        <%= @user.name %>
    </p>
    <p>
        introduction
        <%= @user.introduction%>
    </p>
    <%= link_to "edit", edit_user_path(@user) %>    
    


  <h1>New book</h1>
  <%= form_with model:@book,local: true do |f| %>

  <label for="inputTitle">Title</label>
  <%= f.text_field :title, autofucus: true, id:"inputTitle"  %>

  <label for="inputOpinion">Opinion</label>
  <%= f.text_area :body, autofucus: true, id:"inputOpinion"  %>
  
  <% end %>